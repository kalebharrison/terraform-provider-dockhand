name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  actions: write
  pull-requests: write

jobs:
  update_release_draft:
    name: Update release draft
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Draft release notes
        uses: release-drafter/release-drafter@v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  prepare-compat:
    name: Ensure Dockhand Compat green on main
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Ensure Dockhand Compat succeeded on main
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 scripts/ensure_release_watch_green.py --retries 1 --json

  evaluate-release:
    name: Evaluate release gate
    needs: prepare-compat
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.gate.outputs.ready }}
      version: ${{ steps.gate.outputs.version }}
      tag: ${{ steps.gate.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Evaluate release gate for tagging
        id: gate
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          if ! /usr/bin/python3 scripts/release_gate_check.py --mode tag --json > /tmp/release-gate.json; then
            echo "ready=false" >> "${GITHUB_OUTPUT}"
            cat /tmp/release-gate.json || true
            exit 0
          fi
          {
            echo "ready=true"
            echo "tag=$(jq -r '.tag' /tmp/release-gate.json)"
            echo "version=$(jq -r '.version' /tmp/release-gate.json)"
          } >> "${GITHUB_OUTPUT}"

  publish-artifacts:
    name: Build and publish release artifacts
    needs: evaluate-release
    if: github.event_name == 'workflow_dispatch' && needs.evaluate-release.outputs.ready == 'true'
    permissions:
      contents: write
      id-token: write
      attestations: write
    uses: ./.github/workflows/release-artifacts.yml
    with:
      version: ${{ needs.evaluate-release.outputs.version }}
    secrets: inherit

  housekeeping:
    name: Label released issues and cut CHANGELOG
    needs: [evaluate-release, publish-artifacts]
    if: github.event_name == 'workflow_dispatch' && needs.evaluate-release.outputs.ready == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve release URL
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.evaluate-release.outputs.tag }}
        shell: bash
        run: |
          set -euo pipefail
          url="$(gh release view "${TAG}" --repo "${GITHUB_REPOSITORY}" --json url -q .url)"
          echo "url=${url}" >> "${GITHUB_OUTPUT}"

      - name: Label awaiting-release issues and cut CHANGELOG
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.evaluate-release.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 scripts/release_housekeeping.py \
            --version "${VERSION}" \
            --cut-changelog \
            --commit-changelog \
            --json

      - name: Comment on released issues
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.evaluate-release.outputs.tag }}
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 scripts/release_issue_notify.py --tag "${TAG}" --json
