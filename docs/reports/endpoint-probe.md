# Endpoint Probe Report

Generated by `scripts/endpoint-probe.py`. Regenerate with:

```bash
./scripts/verify.sh --endpoint-probe
```

Requires `DOCKHAND_ENDPOINT`, `DOCKHAND_USERNAME`, and `DOCKHAND_PASSWORD`. After client route changes, update `scripts/endpoint-probe.py` first, then refresh this report.

> **Note:** Counts below reflect the last live probe run. The probe list in git may differ until re-run against current Dockhand.

- Total endpoints: 131
- Present (non-404): 120
- Not present (404 on non-parameterized route): 2
- Unverified (missing fixture for parameterized route): 3
- Unexpected 404 (parameterized route with fixture): 6

## Not Present

- `GET /api/configs`
- `GET /api/backups`

## Unverified (No Fixture)

- `GET /api/config-sets/{id}`
- `GET /api/notifications/{id}`
- `GET /api/git/stacks/{id}/env-files`

## Unexpected 404

- `PUT /api/users/{id}`
- `DELETE /api/users/{id}`
- `PUT /api/environments/{id}`
- `DELETE /api/environments/{id}`
- `POST /api/git/stacks/{id}/deploy-stream`
- `POST /api/git/stacks/{id}/env-files`

